# Optional, but strongly recommended. Without a token GitHub allows only 60 requests/hour.
# Use a CLASSIC token with the `public_repo` scope so Star history (/stargazers) works.
# Fine-grained PATs get 5000 req/h but cannot access /stargazers (returns 403).
# Create one at https://github.com/settings/tokens
GITHUB_TOKEN=
